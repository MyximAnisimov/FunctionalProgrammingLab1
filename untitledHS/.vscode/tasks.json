{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Haskell",
            "type": "shell",
            "command": "ghc",
            "args": [
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}",
                "${Main}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$haskell"
            ]
        }
    ]
}